# ── Binários compilados ──────────────────────────────────────────────────────
backend/clubbix.exe
backend/*.exe
backend/bin/
backend/dist/
backend/tmp/
backend/temp/

# ── Banco de dados ───────────────────────────────────────────────────────────
backend/*.db-journal
backend/*.sqlite
backend/*.sqlite3
clubbix.db
# backend/clubbix.db é commitado intencionalmente para deploy no Railway

# ── Variáveis de ambiente (NUNCA subir para o git) ───────────────────────────
.env
.env.local
.env.production
.env.development
backend/.env
backend/.env.local
# .env.example é permitido (só tem placeholders, sem valores reais)
!.env.example

# ── Go ───────────────────────────────────────────────────────────────────────
backend/vendor/
backend/.gocache/

# ── Configs locais de editor/IDE ─────────────────────────────────────────────
frontend/.claude/
.claude/
.vscode/
.idea/
*.code-workspace

# ── Sistema operacional ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
